NUMEL	DCD		10
LISTA	DCD		1,3,7,3,-4,-5,1,10,3,6
VALSER	DCD		3
NUMOCC	FILL		4
		
		MOV		R2, #NUMEL
		LDR		R2, [R2]
		MOV		R3, #LISTA
		MOV		R4, #VALSER
		LDR		R4, [R4]
		MOV		R5, #NUMOCC
		
		MOV		R6, #0
		
CICLO	LDR		R7, [R3],#4
		CMP		R4, R7
		BNE		NEXTSTEP
		ADD		R6, R6, #1
NEXTSTEP	SUBS		R2, R2, #1
		BGT		CICLO
		
FINE		STR		R6, [R5]
		
		END
