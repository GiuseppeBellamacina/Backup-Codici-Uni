NUMEL	DCD		10
LISTA	DCD		1,3,7,3,-4,-5,1,10,3,6
MIN		FILL		4
MAX		FILL		4
		
		MOV		R2, #NUMEL
		LDR		R2, [R2]
		MOV		R3, #LISTA
		MOV		R4, #MIN
		MOV		R5, #MAX
		
		LDR		R6, [R3]
		LDR		R7, [R3]
		
CICLO	LDR		R8, [R3],#4
CALCMIN	CMP		R6, R8
		BLE		CALCMAX
		MOV		R6, R8
CALCMAX	CMP		R7, R8
		BGE		NEXTSTEP
		MOV		R7, R8
NEXTSTEP	SUBS		R2, R2, #1
		BGT		CICLO
		
FINE		STR		R6, [R4]
		STR		R7, [R5]
		
		END
